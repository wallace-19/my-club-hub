# TODO - Password Reset Flow

- [x] Add DB migration for `password_reset_tokens` table
- [x] Implement `/forgot-password` route + template
- [x] Implement `/reset-password/<token>` route + template
- [x] Update `templates/login.html` to include “Forgot password?” link
- [ ] Sanity test: request reset + change password + login


